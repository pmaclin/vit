<div class="col-md-8 col-md-offset-0">
  <h2><%= @job.title %></h2>
  <p><%= @job.description %></p>

  <div class="col-md-4 col-md-offset-8">
    <%= link_to "Back", root_path %>
    <%= link_to "Edit", edit_job_path(@job) %>
    <%= link_to "Delete", job_path(@job), method: :delete, data: { confirm: "Wait! Are you sure?!?"} %>
  </div>

</div>
